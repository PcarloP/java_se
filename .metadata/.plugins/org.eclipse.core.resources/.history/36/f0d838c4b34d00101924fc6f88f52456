/* Impostazioni globali */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f0f2f5;
    color: #333;
    margin: 0;
    padding: 0;
}

/* Titoli */
h1, h2 {
    text-align: center;
    color: #2c3e50;
    margin-top: 30px;
}

/* Contenitore principale dei form */
.forms-container {
    width: 90%;
    max-width: 800px;
    margin: 40px auto;
    padding: 30px;
    background-color: #ffffff;
    border-radius: 10px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
}

/* Ogni form è disposto in colonna con spazio tra i campi */
form {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    margin-bottom: 20px;
    gap: 10px;
}

/* Etichette */
label {
    font-weight: bold;
}

/* Campi input */
input[type="text"],
input[type="number"] {
    padding: 10px;
    width: 220px;
    border: 1px solid #ccc;
    border-radius: 6px;
    font-size: 1em;
    transition: border-color 0.3s ease;
}

input[type="text"]:focus,
input[type="number"]:focus {
    border-color: #2980b9;
    outline: none;
}

/* Bottoni */
button {
    padding: 10px 18px;
    background-color: #2980b9;
    color: white;
    border: none;
    border-radius: 6px;
    font-size: 1em;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #1c5980;
}
